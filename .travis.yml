language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-lo' ]; then cd .. && eval "mv $currentfolder generator-lo" && cd generator-lo; fi

